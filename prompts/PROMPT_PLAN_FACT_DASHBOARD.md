# 💬 Стартовый Промт для Codex: Дашборд План-Факт ТЗ (PlanFact_Dashboard.html)

Скопируйте данный промт и отправьте его в Codex / VS Code при инициативе работы над дашбордом План-Факт.

---

```text
Ты — Senior Full-Stack & Data Visualization Engineer.

Твоя задача — создать / усовершенствовать генератор автономного аналитического дашборда `PlanFact_Dashboard.html` в папке `plan_fact_dashboard/`.

Перед началом работы обязательно изучи контекстные документы:
1. `shared_context/OVERALL_CONTEXT.md` — бизнес-логика 5 интервалов, структура дампов TS, SLA Предварительного анализа.
2. `plan_fact_dashboard/PRD.md` — спецификация дашборда, фильтры, графики и структура таблицы.
3. `AGENTS.md` — требования к чистоте и автономности кода.

Основные требования к реализации:
1. Python Пайплайн (генератор):
   - Написать Python-скрипт `generate_dashboard.py`, принимающий на вход дамп `ts_export.json`.
   - Произвести асинхронную/оптимизированную агрегацию записей списаний, сгруппировав их по 5 интервалам (ПО, ОА, Разработка, Приемка, Стабилизация, Управление) для каждого запроса.
   - Рассчитать отклонения План vs Факт и флаги нарушения SLA (ПО > 5 дней).
   - Сгенерировать единый автономный HTML-файл `PlanFact_Dashboard.html` с вшитыми данными (JSON data island).
2. Дашборд (Frontend):
   - Выполнить в темной корпоративной теме (Neon Dark / Modern UI) со 60 FPS рендерингом при фильтрации.
   - Панель фильтров: по Заказчику/Банку, Проекту, Статусу (S1-S6), Менеджеру, Нарушению SLA и текстовому поиску (`#1726097`).
   - Визуализация: Интерактивные графики (Plotly.js / Chart.js) — сопоставление Плана и Факта по 5 фазам, гистограмма отклонений и индикаторы SLA.
   - Таблица деталей: Сворачиваемая таблица запросов с подсветкой отклонений (зеленый/желтый/красный).

Обеспечь 100% автономность итогового HTML-файла: он должен открываться локально в любом браузере без сервера и интернета.
```
